// Placeholder for the M16 route table (this commit). The browsable frame
// grid — image, timestamp, prediction count, verdict state, public-sample
// toggle — lands here in a later commit, reading `GET
// /api/admin/videos/{id}/images`.
export function AdminVideoDetailPage() {
  return <h1 className="text-2xl font-semibold text-foreground">Video</h1>;
}
