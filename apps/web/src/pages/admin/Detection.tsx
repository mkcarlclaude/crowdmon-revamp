// Placeholder for the M16 route table (this commit). Prelabel coverage per
// video lands here in a later commit — read-only on purpose; the plan's
// "scope line" section is why a re-run button is not part of this milestone.
export function AdminDetectionPage() {
  return <h1 className="text-2xl font-semibold text-foreground">Detection</h1>;
}
