// Placeholder for the M16 route table (this commit). `SubmitForm` and
// `JobList` move here from the deleted `Admin.tsx` in the next commit,
// unchanged past their own restyle onto shadcn primitives.
export function AdminVideosPage() {
  return <h1 className="text-2xl font-semibold text-foreground">Videos</h1>;
}
