import { GroundTruthSession } from "../../components/GroundTruthSession";

/**
 * `/admin/annotate` (M26, #176): the frozen pool's exhaustive labelling
 * sitting. `AdminVerifyPage`'s own shape — a bare mount, full width.
 */
export function AdminAnnotatePage() {
  return <GroundTruthSession />;
}
