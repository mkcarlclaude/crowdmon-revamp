// Placeholder for the M16 route table (this commit). `LabellingSession`
// moves here from the deleted `Admin.tsx` in the next commit, full width.
export function AdminVerifyPage() {
  return <h1 className="text-2xl font-semibold text-foreground">Verify</h1>;
}
