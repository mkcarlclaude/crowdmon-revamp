// Placeholder for the M16 route table (this commit). `LabellingStats` moves
// here from the deleted `Admin.tsx` and the new `GET /api/admin/verdicts`
// list lands below it in a later commit — "what did I label" and "how much
// is labelled" are one question asked twice (the plan's own words).
export function AdminAnnotationsPage() {
  return <h1 className="text-2xl font-semibold text-foreground">Annotations</h1>;
}
